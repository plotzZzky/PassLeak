/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  reactStrictMode: true,
}

module.exports =  {
  assetPrefix: './',
  basePath: '/PassLeak',
  trailingSlash: true,
}
